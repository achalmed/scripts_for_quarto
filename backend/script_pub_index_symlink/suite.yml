# suite.yml — contrato de la suite (esquema en core/suite.schema.yml); core/suites.py generar escribe los índices y READMEs.
nombre: pub_index_symlink
objetivo: publicacion
resumen: Mantiene 04 index/_indice con enlaces simbólicos por año a cada carpeta de publicación de los blogs.
escribe_en:
- vault
simula_por_defecto: false
comandos:
- main.sh
- main.sh --dry-run
- main.sh --check-broken
- main.sh --clean-broken
estado: activo
depende_de:
- bash
